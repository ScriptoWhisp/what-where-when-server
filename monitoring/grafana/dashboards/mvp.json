{
  "uid": "www-mvp",
  "title": "What Where When — MVP",
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "5s",
  "tags": ["mvp"],
  "time": { "from": "now-15m", "to": "now" },
  "panels": [
    {
      "type": "stat",
      "title": "Targets up",
      "gridPos": { "x": 0, "y": 0, "w": 6, "h": 4 },
      "targets": [
        { "expr": "sum(up)", "refId": "A" }
      ],
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } }
    },
    {
      "type": "timeseries",
      "title": "HTTP RPS (all routes)",
      "gridPos": { "x": 6, "y": 0, "w": 18, "h": 8 },
      "targets": [
        { "expr": "sum(rate(http_requests_total[1m]))", "refId": "A" }
      ]
    },
    {
      "type": "timeseries",
      "title": "HTTP req/min (all routes)",
      "gridPos": { "x": 6, "y": 8, "w": 9, "h": 6 },
      "targets": [
        { "expr": "sum(increase(http_requests_total[1m]))", "refId": "A" }
      ]
    },
    {
      "type": "timeseries",
      "title": "HTTP avg latency (seconds)",
      "gridPos": { "x": 15, "y": 8, "w": 9, "h": 6 },
      "targets": [
        {
          "expr": "sum(rate(http_request_duration_seconds_sum[1m])) / sum(rate(http_request_duration_seconds_count[1m]))",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "HTTP p95 latency (seconds)",
      "gridPos": { "x": 0, "y": 4, "w": 12, "h": 10 },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (le) (rate(http_request_duration_seconds_bucket[1m])))",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "HTTP 5xx rate",
      "gridPos": { "x": 12, "y": 4, "w": 12, "h": 10 },
      "targets": [
        {
          "expr": "sum(rate(http_requests_total{status=~\"5..\"}[1m]))",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Postgres connections",
      "gridPos": { "x": 0, "y": 14, "w": 12, "h": 8 },
      "targets": [
        { "expr": "pg_stat_activity_count", "refId": "A" }
      ]
    },
    {
      "type": "timeseries",
      "title": "Redis ops/sec",
      "gridPos": { "x": 12, "y": 14, "w": 12, "h": 8 },
      "targets": [
        { "expr": "rate(redis_commands_processed_total[1m])", "refId": "A" }
      ]
    },
    {
      "type": "timeseries",
      "title": "Host CPU (node_exporter)",
      "gridPos": { "x": 0, "y": 22, "w": 12, "h": 8 },
      "targets": [
        {
          "expr": "100 - (avg by(instance) (rate(node_cpu_seconds_total{mode=\"idle\"}[1m])) * 100)",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "percent"
        },
        "overrides": []
      }
    },
    {
      "type": "timeseries",
      "title": "Node.js event loop lag (seconds)",
      "gridPos": { "x": 12, "y": 22, "w": 12, "h": 8 },
      "targets": [
        {
          "expr": "nodejs_eventloop_lag_seconds",
          "refId": "A"
        }
      ]
    }
  ]
}

